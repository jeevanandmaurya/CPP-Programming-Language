#include <bits/stdc++.h> //Includes all necessary header files
#include <iostream>      // Input/Output
#include <vector>        // Dynamic array
#include <array>         // Fixed-size array
#include <deque>         // Double-ended queue
#include <list>          // Doubly linked list
#include <set>           // Ordered set
#include <unordered_set> // Unordered set (HashSet)
#include <map>           // Ordered map (TreeMap)
#include <unordered_map> // Unordered map (HashMap)
#include <queue>         // Priority queue, Queue
#include <stack>         // Stack
#include <algorithm>     // Sorting, min/max, binary search
#include <cmath>         // Math functions
#include <numeric>       // GCD, LCM, accumulate
#include <bitset>        // Bit manipulation
#include <sstream>       // String processing
